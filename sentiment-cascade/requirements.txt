# Service
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3

# LLM stage (Claude)
anthropic>=0.69

# Local fast classifier
transformers>=4.40
torch>=2.2            # CPU build: pip install torch --index-url https://download.pytorch.org/whl/cpu
langdetect>=1.0
# spacy + en_core_web_sm are OPTIONAL (enable NER name masking); the service runs without them.

# Tests / eval
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
pandas>=2.0
